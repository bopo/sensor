#!/usr/bin/env sh
# DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"

# cd $DIR/mosquitto-auth-plug
# mv auth-plug.so /etc/mosquitto/

# cd ..
rm -fr mosquitto-auth-plug org.eclipse.mosquitto

# cp $DIR/auth_plugin.conf /etc/mosquitto/conf.d/auth_plugin.conf
